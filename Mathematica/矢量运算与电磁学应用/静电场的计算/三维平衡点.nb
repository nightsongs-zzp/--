(* 1. 定义电场为零的核心方程 *)
eq = z/(1 + z^2)^(3/2) == 1/(z - 1)^2;

(* 2. 可视化方程两边的函数，辅助确定根的大致范围 *)
Plot[{z/(1 + z^2)^(3/2), 1/(z - 1)^2}, {z, 1.5, 3}, 
 PlotLegends -> {"左边函数", "右边函数"},
 AxesLabel -> {"z", "函数值"},
 PlotTitle -> "电场零点方程两边函数对比"];

(* 3. 用FindRoot求解数值解（指定初值，基于图像判断初值选2） *)
solution = FindRoot[eq, {z, 2}];

(* 4. 输出精确的数值解 *)
Print["电场零点的z坐标为：", z /. solution];

(* 5. 验证解的正确性：代入原方程，检查两边是否近似相等 *)
leftValue = z/(1 + z^2)^(3/2) /. solution;
rightValue = 1/(z - 1)^2 /. solution;
Print["方程左边值：", leftValue];
Print["方程右边值：", rightValue];
Print["两边差值：", Abs[leftValue - rightValue]];